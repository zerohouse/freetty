app.post('/api/user', function (req, res) {
    var user = new User(req.passed);
    user.save(function (err, result) {
        var res = {};
        res.result = result;
        res.err = err;
        res.send(res);
    });
});
